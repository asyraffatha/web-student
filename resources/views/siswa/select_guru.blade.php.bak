<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Pilih Guru</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen flex items-center justify-center">

    <div class="w-full max-w-3xl bg-white rounded-xl shadow-lg p-6">
        <h2 class="text-3xl font-semibold text-gray-800 mb-6 text-center">
            👨‍🏫 Pilih Guru untuk Diskusi
        </h2>

        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
            @if ($gurus->isEmpty())
                <p class="col-span-full text-center text-gray-500">Tidak ada guru tersedia.</p>
            @else
                @foreach ($gurus as $guru)
                    <a href="{{ route('discussion.show', $guru->id) }}"
                        class="group block p-4 bg-green-50 hover:bg-green-100 rounded-lg shadow border border-transparent hover:border-green-400 transition duration-200">
                        <div class="flex items-center space-x-3">
                            <div
                                class="w-10 h-10 bg-green-600 text-white flex items-center justify-center rounded-full text-sm font-bold group-hover:scale-105 transform transition">
                                {{ strtoupper(substr($guru->name, 0, 2)) }}
                            </div>
                            <span class="text-green-800 font-medium group-hover:underline">{{ $guru->name }}</span>
                        </div>
                    </a>
                @endforeach
            @endif
        </div>
    </div>

</body>

</html>
